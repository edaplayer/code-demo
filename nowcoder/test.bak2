/*************************************************************************
  > File Name: test.c
  > Author: Edward.Tang
  > Mail:   @163.com 
  > Created Time: Sun 01 Nov 2020 05:41:29 AM PST
 ************************************************************************/

#include <stdio.h>
#include <string.h>
int main(int argc, char **argv)
{
	int n = 0;
	char c = 0;
	while(1)
	{
		c = getchar();
		if(c==EOF)
			break;
		if(c>'0' && c<'9')
		{
			printf("c = %c\n", c);
		} else {
			printf("c(hex) = 0x%x\n", c);
		}
		n++;
		/* printf("n = %d\n", n); */
		/* char * s = argv[n-1]; */
		/* printf("s = %s\n", s); */
		/* int len = strlen(s); */
		/* printf("len = %d\n", len); */
	}
}
